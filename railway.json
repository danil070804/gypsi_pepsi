{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/.cache || true; NPM_CONFIG_PRODUCTION=false npm ci --include=dev --no-audit --no-fund || NPM_CONFIG_PRODUCTION=false npm install --include=dev --no-audit --no-fund; npm run build"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.mjs",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
